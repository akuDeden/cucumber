# tables Snapshots

Halaman Tables — view tabel umum (bukan Advanced Table).

| File | Tab Aktif | Keterangan |
|------|-----------|------------|
| `tables-page.yml` | Tables (umum) | Halaman Tables — state default |
| `rois-table.yml` | ROIs | Tabel ROI — semua ROI di cemetery |

**Entry point**: Dashboard > tombol Tables di nav bar
